spring:
  application:
    name: eureka-server
server:
  port: 8101
  servlet:
    context-path: /eureka-server
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
